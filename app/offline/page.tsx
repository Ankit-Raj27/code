export default function OfflinePage() {
  return (
    <section className="card">
      <p className="eyebrow">Offline mode</p>
      <h2>You can still log locally.</h2>
      <p>
        Previously loaded pages remain available. New entries save on-device first and sync when
        you reconnect.
      </p>
    </section>
  );
}
